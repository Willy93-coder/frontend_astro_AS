---
import LayoutDataDiscover from '../layouts/LayoutDataDiscover.astro';

const resEntities = await fetch('http://www.anysolution.org:1027/v2/entities?limit=1000');
const entities = await resEntities.json();

const resSubscriptions = await fetch('http://www.anysolution.org:1027/v2/subscriptions/');
const subscriptions = await resSubscriptions.json();

const msgInfo = "Ese panel es donde muestran mensajes de error cuando se interactua en los botones de suscripciones";
---

<LayoutDataDiscover title="Welcome to Astro.">
	<main>
		<svg
			class="astro-a"
			width="495"
			height="623"
			viewBox="0 0 495 623"
			fill="none"
			xmlns="http://www.w3.org/2000/svg"
			aria-hidden="true"
		>
			<path
				fill-rule="evenodd"
				clip-rule="evenodd"
				d="M167.19 364.254C83.4786 364.254 0 404.819 0 404.819C0 404.819 141.781 19.4876 142.087 18.7291C146.434 7.33701 153.027 0 162.289 0H332.441C341.703 0 348.574 7.33701 352.643 18.7291C352.92 19.5022 494.716 404.819 494.716 404.819C494.716 404.819 426.67 364.254 327.525 364.254L264.41 169.408C262.047 159.985 255.147 153.581 247.358 153.581C239.569 153.581 232.669 159.985 230.306 169.408L167.19 364.254ZM160.869 530.172C160.877 530.18 160.885 530.187 160.894 530.195L160.867 530.181C160.868 530.178 160.868 530.175 160.869 530.172ZM136.218 411.348C124.476 450.467 132.698 504.458 160.869 530.172C160.997 529.696 161.125 529.242 161.248 528.804C161.502 527.907 161.737 527.073 161.917 526.233C165.446 509.895 178.754 499.52 195.577 500.01C211.969 500.487 220.67 508.765 223.202 527.254C224.141 534.12 224.23 541.131 224.319 548.105C224.328 548.834 224.337 549.563 224.347 550.291C224.563 566.098 228.657 580.707 237.264 593.914C245.413 606.426 256.108 615.943 270.749 622.478C270.593 621.952 270.463 621.508 270.35 621.126C270.045 620.086 269.872 619.499 269.685 618.911C258.909 585.935 266.668 563.266 295.344 543.933C298.254 541.971 301.187 540.041 304.12 538.112C310.591 533.854 317.059 529.599 323.279 525.007C345.88 508.329 360.09 486.327 363.431 457.844C364.805 446.148 363.781 434.657 359.848 423.275C358.176 424.287 356.587 425.295 355.042 426.275C351.744 428.366 348.647 430.33 345.382 431.934C303.466 452.507 259.152 455.053 214.03 448.245C184.802 443.834 156.584 436.019 136.218 411.348Z"
				fill="url(#paint0_linear_1805_24383)"></path>
			<defs>
				<linearGradient
					id="paint0_linear_1805_24383"
					x1="247.358"
					y1="0"
					x2="247.358"
					y2="622.479"
					gradientUnits="userSpaceOnUse"
				>
					<stop stop-opacity="0.9"></stop>
					<stop offset="1" stop-opacity="0.2"></stop>
				</linearGradient>
			</defs>
		</svg>
		<h1>Welcome to <span class="text-gradient">ANYSOLUTION DEV</span></h1>
		<h2><a href="/">Peticiones API</a></h2>
		<h2 style="text-align: center;">Entidades del Orion Context Broker</h2>
		<p class="errormsg">{msgInfo}</p>
		
		<button class="subscription-toggleButton">Mostrar suscripciones</button>
		<table class="subscription-table" style="overflow: hidden;">
			<thead>
				<tr class='even-row'>
					<th>SubscriptionID</th>
					<th>Descripción</th>
					<th>Status</th>
					<th>Número de veces enviadas</th>
					<th>Última notificación</th>
					<!-- Agrega las otras columnas aquí -->
				</tr>
			</thead>
			<tbody>
			{
				subscriptions.map((subscription) => (
					<tr>
						<td>
							<button class="btnDeleteSubscriptionClass" 
								value={subscription.id}>
									Eliminar
							</button>
							{subscription.id}
						</td>
						<td>{subscription.description}</td>
						<td>{subscription.status}</td>
						<td>{subscription.notification.timesSent}</td>
						<td>{subscription.notification.lastNotification}</td>
						<!-- Agrega las otras columnas aquí -->
					</tr>
				)
			)}
			</tbody>
		</table>
        <br/>
		<p>
			NOTA: Si el status del parking es libre indicará: <a class="status_free">⬤</a>. Si no lo es, indicará: <a class="status_occupied">⬤</a>
		</p>
		<table class="entity-table">
			<thead>
				<tr class='even-row'>
					<th>ID</th>
					<th>Tipo</th>
					<!-- Agrega las otras columnas aquí -->
				</tr>
			</thead>
			<tbody>
			{
				entities.map((entity) => (
					<tr>
						<td>
							<button class="btnCreateSubscriptionClass" 
								value={JSON.stringify(entity)}>
									Crear subscripción
							</button>
							{entity.id}</td>
						<td>{entity.type}</td>
					</tr>
				)
			)}
			</tbody>
		</table>
	</main>
</LayoutDataDiscover>
<script>
	const BACKEND_PORT = 3000;
	const backendHostUrl = `http://localhost:${BACKEND_PORT}`;
	let msgInfo = "";
			
	const dirGetEntities = `/get-entities`;
	const dirGetSubscriptions = `/get-subscriptions`;
	// TODO: Hacer que se actualice y muestre los datos de forma dinámica (que no sea location.reload())
			
	/***********************************************/
	/************* CREAR SUBSCRIPCION **************/
	/***********************************************/
	const dirCreateSubs = `create-subscription`;
	const notificationWebhookUrl = "http://192.168.1.70:5051";
	const btnCreateSubs = document.querySelectorAll('.btnCreateSubscriptionClass');
	
	async function createSubscription(entity, all) {
		const subscriptionPayload = {
			description: (all) 
				? `Notify me of all this entity type: '${entity.type}' status changes`
				: `Notify me of all ${entity.id} status changes`,
			subject: {
					entities: [
						(all) 
						? 
							{
								idPattern: ".*",
								type: entity.type 
							}
						:
							{
									id: entity.id,
									type: entity.type
							}
						],
						condition: {
							attrs: [ "status" ]
						}
			},
				notification: {
				http: { url: notificationWebhookUrl, }
			}
		};

			const res = await fetch(`${backendHostUrl}/${dirCreateSubs}`, {
				method: 'POST',
				headers: { 'Content-Type': 'application/json' },
				body: JSON.stringify(subscriptionPayload)
			});

			if (!res.ok) {
				console.error(`RESPONSE STATUS: ${res.status}`);
				console.error(`ERROR BACKEND: ${res.text}`);
				throw new Error(`Failed to send info to the backend: ${res.text}`);
			}
				
			return res;
		}

			btnCreateSubs.forEach( (element) => {
				element.addEventListener( "click", async (e) => {
					e.preventDefault();
					const selectedEntity = JSON.parse(e.target.value);
					
					try {
						console.log(`Starting... SELECTED ENTITY TO CREATE A NEW SUBSCRIPTION: ${selectedEntity}`);
						const res = await createSubscription(selectedEntity, false);
						
						if ( res.ok ) {
							msgInfo = "LA SUBSCRIPCIÓN HA SIDO CREADA!";
							console.log("RESPONSE STATUS: " + res.status);
							console.log(`${msgInfo}`);
							
							//await getDataAndRender();
							location.reload();
						}
						
					}
					catch (e) {
						console.error(`BUTTON CREATE SUBSCRIPTION CATCH ERROR: ${e}`);
					}
				});
			});


			/**************************************************/
			/************* ELIMINAR SUBSCRIPCION **************/
			/**************************************************/
			const dirDeleteSubs = `delete-subscription`;
			const btnDeleteSubs = document.querySelectorAll('.btnDeleteSubscriptionClass');
			async function deleteSubscription(subscriptionId) {
				const r = await fetch(`${backendHostUrl}/${dirDeleteSubs}`, {
					method: 'POST',
					headers: {'Content-Type': 'application/json'},
					body: JSON.stringify({ "id": subscriptionId }),
				});

				// console.log(`backendHostUrl: ${backendHostUrl}/${dirDeleteSubs}`);
				// console.log(`JSON stringify: ${JSON.stringify({ "id": subscriptionId })}`);
				// console.log(`R STATUS: ${r.status}`);

				if (!r.ok) {
					console.error(`RESPONSE STATUS: ${r.status}`);
					console.error(`ERROR BACKEND: ${r.text}`);
					throw new Error(`Failed to send info to the backend: ${r.text}`);
				}

				return r;
			}

			btnDeleteSubs.forEach( (element) => {
				element.addEventListener( "click", async (e) => {
					e.preventDefault();
					const selectedSubscriptionId = e.target.value;
					try {
						console.log(`Starting... SELECTED SUBSCRIPTIONID TO BE DELETED: ${selectedSubscriptionId}`);
						const res = await deleteSubscription(selectedSubscriptionId);
						
						if ( res.ok ) {
							msgInfo = "LA SUBSCRIPCIÓN HA SIDO ELIMINADA!";

							console.log("RESPONSE STATUS: " + res.status);
							console.log(`${msgInfo}`);
							
							//await getDataAndRender();
							location.reload();
						}
					}
					catch (e) {
						console.error(`BUTTON DELETE CATCH ERROR: ${e}`);
					}
				});
			});
</script>

<style>
	main {
		margin: auto;
		padding: 1rem;
		width: 800px;
		max-width: calc(100% - 2rem);
		color: white;
		font-size: 20px;
		line-height: 1.6;
	}
	.astro-a {
		position: absolute;
		top: -32px;
		left: 50%;
		transform: translatex(-50%);
		width: 220px;
		height: auto;
		z-index: -1;
	}
	h1 {
		font-size: 4rem;
		font-weight: 700;
		line-height: 1;
		text-align: center;
		margin-bottom: 1em;
	}
	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
	.instructions {
		margin-bottom: 2rem;
		border: 1px solid rgba(var(--accent-light), 25%);
		background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%));
		padding: 1.5rem;
		border-radius: 8px;
	}
	.instructions code {
		font-size: 0.8em;
		font-weight: bold;
		background: rgba(var(--accent-light), 12%);
		color: rgb(var(--accent-light));
		border-radius: 4px;
		padding: 0.3em 0.4em;
	}
	.instructions strong {
		color: rgb(var(--accent-light));
	}
	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
		gap: 2rem;
		padding: 0;
	}
	.errormsg {
		max-width: calc(100% - 2rem);
		border: 1px solid rgba(var(--accent-light), 25%);
		background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%));
		color: #ff0000;
		text-shadow: #4e0000;
		border-radius: 5px;
		font-size: 30px;
		text-align: center;
	}
	.btnSubscription {
		max-width: calc(100% - 2rem);
		border: 1px solid rgba(var(--accent-light), 25%);
		background: linear-gradient(#bbff00, #faf600);
		color: #00319c;
		border-radius: 5px;
		font-size: 30px;
		text-align: center;
	}

/* TABLA DE ENTIDADES */
	.entity-table {
        width: 100%;
        border-collapse: collapse;
    }

    .entity-table th,
    .entity-table td {
        padding: 12px;
        border-bottom: 1px solid #555;
        text-align: left;
    }

    .entity-table th {
        background-color: #333;
    }

    .entity-table tbody tr:nth-child(even) {
        background-color: #2f2f2f;
    }

    .entity-table .even-row {
        background-color: #2f2f2f;
    }

    .entity-table .odd-row {
        background-color: #3f3f3f;
    }

	.status_free {
		text-align: center;
        color: #00fa00;
    }
	.status_occupied {
		text-align: center;
        color: #ff0000;
    }

/* TABLA DE SUBSCRIPCIONES */
	.subscription-table {
        width: 100%;
        border-collapse: collapse;
        margin: 20px 0;
        font-family: Arial, sans-serif;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        border-radius: 8px;
        overflow: hidden;
    }

    .subscription-table th,
    .subscription-table td {
        padding: 12px;
        border-bottom: 1px solid #ddd;
        text-align: left;
    }

    .subscription-table th {
        background-color: #370075;
        color: #fff;
    }

    .subscription-table tbody tr {
        transition: background-color 0.3s ease;
    }

    .subscription-table tbody tr:hover {
        background-color: #0f305a;
    }

    .subscription-table tbody tr:nth-child(even) {
        background-color: #002153;
    }

    .subscription-table .even-row {
        background-color: #00115e;
    }

    .subscription-table .odd-row {
        background-color: #001d7c;
    }
</style>
